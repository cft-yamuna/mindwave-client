{
  "name": "mindwave-football-client",
  "version": "0.1.0",
  "private": true,
  "description": "Client laptop (Player 2): reads the local MindWave sensor and streams it to the master over the LAN.",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "check-sensor": "node check-sensor.js",
    "test": "node --test"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
